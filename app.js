const menu=document.querySelector('.menu');const nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'Close navigation':'Open navigation');nav.classList.toggle('open',open)});nav.addEventListener('click',e=>{if(e.target.closest('a')){nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}});document.addEventListener('keydown',e=>{if(e.key==='Escape'){nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}});
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
const opening=document.getElementById('opening'),fill=document.getElementById('loading-fill');
let loaded=0;const critical=[...document.querySelectorAll('.opening img,.hero-photo')];const started=performance.now();
function finishOpening(){const delay=reduced?0:Math.max(0,1500-(performance.now()-started));setTimeout(()=>{fill.style.width='100%';setTimeout(()=>{opening.classList.add('done');opening.setAttribute('aria-hidden','true')},reduced?0:350)},delay)}
function assetReady(){loaded++;fill.style.width=`${Math.round(loaded/critical.length*90)}%`;if(loaded===critical.length)finishOpening()}
critical.forEach(img=>{if(img.complete)assetReady();else{img.addEventListener('load',assetReady,{once:true});img.addEventListener('error',assetReady,{once:true})}});setTimeout(()=>{opening.classList.add('done');opening.setAttribute('aria-hidden','true')},5000);
if(!reduced&&'IntersectionObserver' in window){document.documentElement.classList.add('js-motion');const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el))}
