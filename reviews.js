const track=document.querySelector('.review-carousel');if(track){const prev=document.querySelector('.reviews-prev'),next=document.querySelector('.reviews-next');const update=()=>{prev.disabled=track.scrollLeft<=2;next.disabled=track.scrollLeft+track.clientWidth>=track.scrollWidth-2};const move=direction=>{const card=track.querySelector('.review-card');track.scrollBy({left:direction*(card.getBoundingClientRect().width+24),behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'})};prev.addEventListener('click',()=>move(-1));next.addEventListener('click',()=>move(1));track.addEventListener('scroll',update,{passive:true});window.addEventListener('resize',update);update()}